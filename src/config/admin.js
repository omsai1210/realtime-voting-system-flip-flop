/**
 * src/config/admin.js
 *
 * Admin credentials for the frontend-only admin panel.
 * Change these before deploying.
 *
 * NOTE: Because this is a frontend-only app, the admin password is in
 * the JavaScript bundle. This is acceptable for an internal event tool.
 * Do not reuse this password elsewhere.
 */

export const ADMIN_EMAIL = 'abhivriddhi@vit.edu';
export const ADMIN_PASSWORD = 'Abhivriddhi@2026';
